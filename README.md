# PhysicsEngine implemented in C++

It uses sfml as GUI framework.

for installation of sfml run: 
`sudo apt-get install libsfml-dev` 

for building run: `make`
for building and also executing run: `make bar`
